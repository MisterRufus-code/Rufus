# CLAUDE.md

The guidance for this repository lives in **[AGENTS.md](AGENTS.md)** — commands,
architecture, and the conventions that have already cost a real bug.

It is kept vendor-neutral so every coding agent reads the same rules, and it is
the single source of truth on purpose: two guidance files drift apart, and the
one that drifts is the one nobody notices is wrong.

Read `AGENTS.md` before working in this repo.
